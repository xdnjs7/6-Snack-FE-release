export type TButtonType = "primary" | "black" | "gray" | "white" | "grayDisabled" | "whiteDisabled";

export type TButtonStyle = {
  bg: string;
  textColor?: string;
  padding: string;
  border?: string;
  font?: string;
  size?: string;
};